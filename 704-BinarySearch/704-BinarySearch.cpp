        while (s <= e) {
            mid = s + (e-s)/2;
            if (nums[mid]==target) {
        int mid = 0;
        int s= 0, e= nums.size() -1;
            if (nums[0] == target) {
        }
                return 0;
            }
            else {
                return -1;
            }
                return mid;
            }
            else if (nums[mid] > target) {
                e = mid - 1;
            }
[
