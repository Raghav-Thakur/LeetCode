        while (s <= e) {
            mid = s + (e-s)/2;
            if (nums[mid]==target) {
        int mid = 0;
        int s= 0, e= nums.size() -1;
    int search(vector<int>& nums, int target) {
public:
class Solution {
                return mid;
            }
            else if (nums[mid] > target) {
                e = mid - 1;
            }
            else if (nums[mid] < target) {
                s = mid + 1;
            }
[
