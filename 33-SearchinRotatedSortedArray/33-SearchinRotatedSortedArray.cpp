        while (s <= e) {
            mid = s + (e - s) / 2;
            }
            return -1;
        }
            for(int i = 0; i <= size-1; i++) {
                if (nums[i] == target) {
                    return i;
                }
        int mid = 0;
        int ans;
        if (size < 1000) {
    
    int search(vector<int>& nums, int target) {
        int size = nums.size();
        int s = 0, e = size - 1;
        }
        return -1;
    }
[
