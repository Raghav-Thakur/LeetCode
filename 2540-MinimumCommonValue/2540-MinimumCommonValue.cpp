                start = mid + 1;
            }
        }
        return -1;
    }
    
            if (index != -1) {
                return nums1[i];
    int getCommon(const vector<int>& nums1, const vector<int>& nums2) {
        for (int i = 0; i < nums1.size(); ++i) {
            int index = BinarySearch(nums2, nums1[i], nums2.size());
            }
        }
        return -1;
    }
};

[
