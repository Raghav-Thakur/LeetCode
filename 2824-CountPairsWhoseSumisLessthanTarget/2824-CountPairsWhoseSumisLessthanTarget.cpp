        int ans = 0,diff = 0;
                diff = nums[i] + nums[j];
                if (diff < target) {
    int countPairs(vector<int>& nums, int target) {
        for (int i = 0; i < nums.size(); ++i) {
            for (int j = i + 1; j < nums.size(); ++j) {
                }
                    ans++;
            }
        }
        return ans;
class Solution {
public:
    }
};

[
